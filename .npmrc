# Skip Chromium download when installing puppeteer (not needed on Railway/production)
# PDF export is a local-only feature; unset these env vars locally if you need it
PUPPETEER_SKIP_DOWNLOAD=true
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true
puppeteer_skip_download=true
