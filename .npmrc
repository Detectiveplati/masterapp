# Skip Chromium download when installing puppeteer (not needed on Railway/production)
# PDF export is a local-only feature; set PUPPETEER_SKIP_DOWNLOAD=false locally if you need it
PUPPETEER_SKIP_DOWNLOAD=true
